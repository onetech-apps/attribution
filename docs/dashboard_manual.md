# Attribution System — Мануал

## Зміст

1. [Запуск сервера](#1-запуск-сервера)
2. [Огляд дашборду](#2-огляд-дашборду)
3. [Додавання додатку](#3-додавання-додатку)
4. [Отримання API Key](#4-отримання-api-key)
5. [Інтеграція SDK в iOS-додаток](#5-інтеграція-sdk-в-ios-додаток)
6. [Налаштування трекінг-посилань](#6-налаштування-трекінг-посилань)
7. [Моніторинг та логи](#7-моніторинг-та-логи)
8. [Тестування](#8-тестування)

---

## 1. Запуск сервера

### Вимоги
- **Node.js** 18+
- **PostgreSQL** (база даних `attribution_db`)
- **Redis** (кешування кліків)

### Налаштування `.env`

```env
PORT=3000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_NAME=attribution_db
DB_USER=postgres
DB_PASSWORD=your_password

REDIS_HOST=localhost
REDIS_PORT=6379

API_SECRET_KEY=your_secret_key
```

### Запуск

```bash
# Встановити залежності
npm install

# Запустити в режимі розробки (з автоперезапуском)
npm run dev

# Або для продакшну
npm run build && npm start
```

Сервер стартує на `http://localhost:3000`. При першому запуску автоматично створюються всі таблиці в БД.

---

## 2. Огляд дашборду

Дашборд доступний за адресою: `http://localhost:3000/dashboard.html`

### Навігація (4 вкладки)

| Вкладка | URL | Опис |
|---------|-----|------|
| **Дашборд** | `/dashboard.html` | Статистика, кліки, атрибуції, Live Log |
| **Логи** | `/dashboard.html?tab=logs` | Логи постбеків та помилок |
| **Тестування** | `/testing.html` | Перевірка посилань, симулятори |
| **Додатки** | `/apps.html` | Управління додатками (CRUD) |

### Дашборд — головна сторінка

- **4 статистичні картки**: всього кліків, атрибуцій, за 24 години, conversion rate
- **Live Event Log**: реалтайм потік подій (кліки, атрибуції, постбеки, помилки)
- **Таблиця кліків**: останні 20 кліків з деталями
- **Таблиця атрибуцій**: останні 20 атрибуцій
- **Live Updates**: перемикач автооновлення (кожні 10 секунд)

---

## 3. Додавання додатку

> **Кожний iOS-додаток, який використовує attribution system, потрібно зареєструвати як "додаток" в системі.**

### Крок за кроком

1. Перейти на вкладку **Додатки** (`/apps.html`)
2. Натиснути **"Додати додаток"** (зелена кнопка)
3. Заповнити форму:

| Поле | Приклад | Опис |
|------|---------|------|
| **App ID** * | `com.company.myapp` | Унікальний ідентифікатор додатку |
| **Домен** * | `track.myapp.com` | Домен для трекінг-посилань |
| **Team ID** * | `ABC123DEF4` | 10-символьний Apple Developer Team ID |
| **Bundle ID** * | `com.company.myapp` | Bundle Identifier з Xcode |
| **Назва** * | `My App` | Назва для відображення |
| **App Store URL** | `https://apps.apple.com/app/id123456` | Посилання на App Store |

4. (Опціонально) Увімкнути **AppsFlyer** для in-app джерел атрибуції (Moloko, Unity, TikTok, Google Ads)
   - Вказати AppsFlyer Dev Key з панелі AppsFlyer

5. Натиснути **"Зберегти"**

> Після створення система автоматично згенерує **API Key** та покаже його на екрані.

---

## 4. Отримання API Key

API Key генерується автоматично при створенні додатку.

### Де знайти API Key

1. У таблиці додатків — натиснути кнопку 📋 (копіювати) біля ключа
2. При **редагуванні** додатку — відображається поле "API Key (Тільки для читання)"

### Зберігання ключа

> ⚠️ **Збережіть API Key одразу!** Він потрібен для:
> - Налаштування SDK в iOS-додатку
> - Авторизації запитів до attribution API

---

## 5. Інтеграція SDK в iOS-додаток

### 5.1. Додати SDK до проекту

Скопіюйте папку `attribution-sdk-ios/Sources/AttributionSDK/` у ваш Xcode проект.

### 5.2. Базова конфігурація (тільки Facebook)

В `AppDelegate.swift`:

```swift
import UIKit

@main
class AppDelegate: UIResponder, UIApplicationDelegate {

    func application(_ application: UIApplication,
                     didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {

        // Конфігурація Attribution SDK
        AttributionSDK.configure(
            apiKey: "ВАШ_API_KEY_З_ДАШБОРДУ",     // ← з кроку 4
            baseURL: "https://track.myapp.com"      // ← домен з кроку 3
        )

        return true
    }
}
```

### 5.3. Конфігурація з AppsFlyer

```swift
AttributionSDK.configure(
    apiKey: "ВАШ_API_KEY",
    baseURL: "https://track.myapp.com",
    appsFlyerDevKey: "ABC123DEF456...",     // ← з панелі AppsFlyer
    appleAppID: "123456789"                 // ← числовий Apple App ID
)
```

Додатково в `SceneDelegate.swift` або `AppDelegate`:

```swift
func sceneDidBecomeActive(_ scene: UIScene) {
    AttributionSDK.shared.startAppsFlyer()
}
```

### 5.4. Отримання атрибуції

Виклик при першому відкритті додатку:

```swift
AttributionSDK.shared.fetchAttribution { result in
    switch result {
    case .success(let data):
        print("✅ Attributed!")
        print("Click ID: \(data.clickId ?? "organic")")
        print("Final URL: \(data.finalUrl ?? "none")")

        // Тут можна використовувати sub1, sub2, fbclid...
        // Наприклад, для показу потрібного контенту

    case .failure(let error):
        print("❌ Attribution failed: \(error)")
        // Organic install — показати дефолтний контент
    }
}
```

---

## 6. Налаштування трекінг-посилань

### Формат посилання

```
https://ВАШ_ДОМЕН/?sub1=КАМПАНІЯ&sub2=ДЖЕРЕЛО&fb_id=PIXEL_ID&fb_token=TOKEN&fbclid={{fbclid}}
```

### Для Facebook Ads:

```
https://track.myapp.com/?sub1=campaign_name&sub2=facebook&fb_id=123456789&fb_token=YOUR_TOKEN&fbclid={{fbclid}}
```

`{{fbclid}}` — макрос Facebook, автоматично підставляє Click ID.

### Генератор URL (в дашборді)

1. Перейти на вкладку **Тестування** → **Ручні інструменти**
2. Обрати додаток зі списку
3. Вказати Facebook Pixel ID та назву кампанії
4. Натиснути **"Копіювати URL"**
5. Вставити URL в Facebook Ads Manager як Destination URL

---

## 7. Моніторинг та логи

### Live Event Log (на дашборді)

Реальний потік подій з кольоровим кодуванням:
- 🔵 **Click** — новий клік зафіксовано
- 🟢 **Attribution** — додаток відкрито, атрибуція знайдена
- 🟣 **Postback** — постбек надіслано
- 🔴 **Error** — помилка

### Вкладка Логи

**Постбеки** — таблиця всіх надісланих постбеків:
- Час, Click ID, HTTP метод, статус відповіді
- Кнопки "Огляд" для перегляду payload та відповіді (JSON)

**Помилки** — таблиця помилок системи:
- Час, тип, повідомлення, деталі (stack trace, metadata)

Обидва розділи мають кнопку **"Очистити"** для видалення старих логів.

---

## 8. Тестування

### Перевірка посилання (вкладка Тестування)

1. Ввести URL трекінг-посилання
2. Натиснути **"Запустити тест"**
3. Система перевірить:
   - ✅ Формат URL
   - ✅ HTTP Redirect (чи правильно працює редірект)
   - ✅ Пошук кліка в базі даних
4. Якщо клік знайдений — з'являється **"Симуляція інсталу"** для тестування атрибуції

### Тест постбеку (Ручні інструменти)

1. Ввести Click ID з бази
2. Натиснути **"Надіслати Postback"**
3. Перевірити результат та переглянути лог на вкладці Логи

### Повний цикл тестування

```
1. Додати додаток в дашборді → отримати API Key
2. Перейти за трекінг-посиланням в браузері
3. Перевірити на дашборді що клік з'явився в таблиці
4. На вкладці Тестування → "Запустити тест" з URL кліка
5. Натиснути "Симулювати Attribution"
6. Перевірити атрибуцію в таблиці на дашборді
7. Надіслати тестовий постбек
8. Перевірити лог постбеків
```

---

## Структура API

| Endpoint | Метод | Опис |
|----------|-------|------|
| `/api/v1/track/click` | GET | Фіксація кліку (+ `/t`, `/click`, `/track`) |
| `/api/v1/attribution` | POST | Запит атрибуції з iOS SDK |
| `/api/v1/postback` | GET | Прийом постбеків |
| `/api/v1/admin/clicks` | GET | Останні кліки (для дашборду) |
| `/api/v1/admin/attributions` | GET | Останні атрибуції |
| `/api/v1/admin/apps` | GET/POST/PUT/DELETE | CRUD додатків |
| `/api/v1/admin/logs/postbacks` | GET/DELETE | Логи постбеків |
| `/api/v1/admin/logs/errors` | GET/DELETE | Логи помилок |
| `/api/v1/clicks/stats` | GET | Статистика кліків |
| `/health` | GET | Health check |
